language: java

services:
  - docker

if: branch = master

before_install:
  - docker build -t app .
  - docker image ls

script:
  docker run app

after_success:
  - echo "TRAVIS BRANCH ${TRAVIS_BRANCH}"
  - test $TRAVIS_BRANCH = master
  - echo "ON MASTER"